# NutriTail Knowledge Gap Asset Log

This log turns repeated findings into durable NutriTail assets. It is not a task dump.
Each entry must say what pattern was found, which permanent asset should own it, and how we will verify it.

## Asset Types

- `rule`: deterministic nutrition, safety, ranking, intake, or formatting logic.
- `dataset`: Food V2, dialogue corpus, golden cases, source registry, or evaluation fixture data.
- `test`: regression, QA script, smoke test, live-case fixture, or launch readiness gate.
- `profile`: brand, ingredient, nutrient, health-condition, or customer-context profile.
- `knowledge_module`: structured veterinary/nutrition knowledge module used by rules or explanation.

## Current Priority Entries

| Gap or pattern | Asset type | Permanent owner | Proposed fix | Verification |
| --- | --- | --- | --- | --- |
| Customer-facing chatbot exposed back-office wording such as review/source/quality labels. | rule, test | `lib/ai/responseComposer.ts`, `lib/food-v2/chatbotRecommendationSummary.ts` | Keep customer answers in plain shopping/nutrition language; keep internal evidence only in admin/report contexts. | `npm run qa:chatbot-customer-recommendations`, `npm run qa:customer-recommendation-presentation-contract` |
| A preferred protein such as salmon was not strong enough to shape the visible shortlist when safe salmon options existed. | rule, test | `lib/food-v2/recommendationRanking.ts` | Prioritize safe customer-visible preferred-protein matches and do not fill visible value alternatives with unrelated proteins while preferred candidates exist; show fewer cards rather than ignoring the stated taste preference. | `npm run qa:food-v2-preference-ranking` |
| Live dog recommendation cases must assert that a clear preferred protein changes the top visible card, not only the extracted facts. | test, dataset | `scripts/qa/run-dog-chatbot-live-cases.ts`, dog live-case fixture bank | Promote flavour-preference bugs into live-case checks such as case 28, where salmon preference must surface a salmon/σολομό option first when safe candidates exist. | `npm run qa:dog-chatbot-live-cases`, `npm run qa:food-v2-preference-ranking` |
| Natural Greek pet-name phrases can be mistaken for the whole name. | rule, test | `lib/petName.ts`, `lib/ai/intakeValidation.ts` | Strip phrases such as "την λένε", "τον λένε", and species-owner name phrases before saving/displaying. | `npm run qa:chatbot-intake-cleanup` |
| Direct lowercase Greek pet names need stable customer-facing capitalization and common accent restoration when the name is known. | rule, test | `lib/petName.ts`, `scripts/qa/check-chatbot-intake-cleanup.ts` | Keep known Greek pet-name normalization covered for direct inputs such as `λεωνιδας`, `μπεμπα`, and `κλεομενης`, not only phrase-based extraction. | `npm run qa:chatbot-intake-cleanup` |
| Brand/food comparison intent may arrive before a pet is selected. | rule, test | `app/account/chatbot/page.tsx`, `app/api/account/foods/compare/route.ts` | Preserve the pending compare request and ask only the missing pet context before comparing. | `npm run qa:chatbot-customer-recommendations` |
| Brand-only comparison such as "Royal Canin ή Acana" can accidentally pick a random formula from each brand. | rule, test | `app/api/account/foods/compare/route.ts`, `app/account/chatbot/page.tsx` | Treat brand-only compare queries as needing an exact formula; show likely formulas as choices, but do not declare a winner until retrieved product rows are explicit. | `npm run qa:chatbot-customer-recommendations` |
| Medical red flags must interrupt shopping flow. | rule, dataset, test | `lib/chatbot/safetyRules.ts`, `data/dialogue-corpus/*/emergency.json` | Convert every urgent symptom pattern into a dialogue corpus entry and a safety regression case. | `npm run qa:chatbot-safety-interrupts`, `npm run qa:dialogue-corpus` |
| Dog cases 101-200 reveal breed, activity, GI, allergy, urinary, renal, senior, and environment patterns. | dataset, test, knowledge_module | `data/evals/chatbot-dog-edge-cases-101-200.json`, `lib/nutrition-v2/*Rules.ts` | Keep cases as a live QA bank; promote failures into rules or dialogue corpus entries. | `npm run qa:dog-edge-fixture`, `npm run qa:dog-chatbot-live-cases` |
| Cat cases need parallel coverage by life stage, sterilisation, urinary/renal, obesity, picky eating, and emergency. | dataset, test | `data/evals/chatbot-extra-cases-cat-001-500.json`, `data/dialogue-corpus/cats` | Keep cat cases grouped by category and turn repeated misses into safety/ranking tests. | `npm run qa:cat-case-fixture`, `npm run qa:cat-chatbot-live-cases` |
| Wrong size or life-stage recommendations are high-trust failures. | rule, test | `lib/food-v2/recommendationRanking.ts` | Hard-hold visible size/life-stage mismatches when suitable alternatives exist. | `npm run qa:food-v2-preference-ranking`, `npm run qa:food-v2-launch-edge-accuracy` |
| Senior pets with chewing or dental difficulty need texture-aware recommendations, not only generic senior formulas. | rule, test | `lib/nutrition-v2/seniorRules.ts`, `scripts/qa/check-gi-allergy-senior-v2.ts` | Boost wet/soft or small-kibble/easy-chew positioning while keeping a texture review caution for dry foods. | `npm run qa:gi-allergy-senior-v2` |
| Nutrient gaps should not become vague customer warnings. | dataset, rule, profile | Food V2 nutrient gap reports, nutrient profiles | Use gaps to drive data-quality queues and internal confidence, while customer copy stays simple. | `npm run qa:food-v2-format-coverage`, `npm run qa:clean-customer-wording-proof` |
| Wet/canned-only requests can pass logic but still expose a Food V2 coverage gap when no visible wet candidates exist. | dataset, test | Food V2 wet-food import queue, dog/cat live case fixtures | Add wet dog and cat foods as a dedicated import wave; keep dry-food recommendations held when the user asks only for wet/canned, and ask for brand + exact formula or label photos so gaps become import candidates. | `npm run qa:dog-chatbot-live-cases`, `npm run qa:cat-chatbot-live-cases`, `npm run qa:missing-format-recommendation-message` |
| Returning customers may need a flavour/brand change even when weight and stool data look stable. | rule, test | `lib/chatbot/progressParsing.ts`, `lib/chatbot/progressDecision.ts` | Parse food acceptance/boredom/refusal as structured progress facts and route them to food-fit review or alternative-food guidance. | `npm run qa:chatbot-progress-parsing`, `npm run qa:chatbot-progress-decision`, `npm run qa:saved-pet-continuation-contract` |
| Returning saved-pet progress checks can receive a minimal answer such as `7κιλα`; this must stay in progress mode instead of falling back to generic pet-selection copy. | rule, test | `app/account/chatbot/page.tsx`, `scripts/qa/check-saved-pet-continuation-contract.ts` | Route progress metrics before the generic `petChoice` fallback and keep one-number current-weight replies usable. | `npm run qa:saved-pet-continuation-contract`, `npm run qa:chatbot-progress-parsing` |
| Customer safety copy should be severity-aware instead of repeating the full red-flag list in every simple recommendation. | rule, test | `lib/ai/responseComposer.ts`, `scripts/qa/check-chatbot-customer-facing-recommendations.ts` | Keep normal food-shopping recommendations light and practical, but preserve strong veterinarian guidance when renal, urinary, diabetes, pancreatitis, vomiting, diarrhea, blood, anorexia, or urinary difficulty appears. | `npm run qa:chatbot-customer-recommendations` |
| Greek customer summaries can still leak small English field labels such as `Budget:` even after the main recommendation copy is localized. | rule, test | `app/account/chatbot/page.tsx`, `scripts/qa/check-chatbot-customer-facing-recommendations.ts` | Treat language-label leaks as customer-copy regressions and keep Greek intake/recommendation summaries fully localized. | `npm run qa:chatbot-customer-recommendations` |
| Recommendation cards can feel uneven when a raw food row has a reason but no caution, or a caution but no reason. | rule, test | `app/account/chatbot/page.tsx`, `scripts/qa/check-chatbot-customer-facing-recommendations.ts` | Every customer-visible food card should always show a clear why-this-choice section and a what-to-watch section, with safe fallback copy when structured payload details are missing. | `npm run qa:chatbot-customer-recommendations` |
| Live customer journey proof can expose real data/ranking gaps, but CI runners often lack authenticated/manual proof inputs. | test, dataset | `scripts/qa/check-customer-live-journey-proof.mjs`, `reports/customer_live_journey_proof_qa.md` | Keep live journey proof strict when authenticated/manual proof is present, but make CI generate a review report instead of blocking unrelated PRs when live proof inputs are absent. Convert visible-card shortages into data/ranking follow-up work. | `npm run qa:customer-live-journey-proof`, `npm run qa:ci-readiness` |
| A food can be correctly held by individual ranking but still needs split-level protection so it never appears as a customer-visible alternative. | rule, test | `lib/food-v2/recommendationRanking.ts`, `scripts/qa/check-food-v2-preference-ranking.ts` | Recommendation splitting must preserve hold decisions, especially active/performance foods in sterilised or weight-sensitive contexts. | `npm run qa:food-v2-preference-ranking` |
| Customer-visible food cards should never expose raw food-intelligence tags such as `_review` or snake_case use cases. | rule, test | `app/account/chatbot/page.tsx`, `scripts/qa/check-chatbot-customer-facing-recommendations.ts` | Keep structured tags for ranking and admin logic, but translate them into short human labels before rendering cards. | `npm run qa:chatbot-customer-recommendations` |
| Repeated brand behavior should become brand intelligence, not ad hoc copy. | profile, knowledge_module | `lib/food-intelligence/profiles.ts` | Store brand strengths, cautions, best use cases, and known data limitations for explanation only. | `npm run qa:food-intelligence-use-cases` |

## Intake Rule

When a new bug, repeated customer phrase, disease nuance, missing nutrient pattern, or brand-specific behavior appears:

1. Add or update one row in this log.
2. Pick the durable asset owner.
3. Add a regression test or identify the existing QA gate.
4. Only then apply the immediate code/data fix.
